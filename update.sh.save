#!/bin/sh
# Stop everything
docker compose down

# Cleanup backend image
docker image remove mc-backend

# Cleanup mcserver config
rm -f ./data/mcserver/server.properties

# Update sources
git pull

# prepare storage


# Rebuild and start everything
docker compose up -d
